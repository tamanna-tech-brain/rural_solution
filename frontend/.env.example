# Optional frontend proxy configuration. If your backend is running on a different host or port,
# set this to the backend API base URL used for translation requests.
# Example: VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_BASE_URL=
